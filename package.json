{
  "name": "NASA-Project",
  "version": "1.0.0",
  "description": "Nasa Project root",
  "main": "index.js",
  "scripts": {
    "install-server": "cd server && yarn install",
    "install-client": "cd client && yarn install",
    "install": "yarn run install-server && yarn run install-client",
    "server": "cd server && yarn run watch",
    "client": "cd client && yarn run start",
    "watch": "npm-run-all --parallel server client",
    "deploy": "cd client && yarn run build && cd ../server && yarn run start",
    "test": "cd server && yarn run test && cd ../client && yarn run test"
  },
  "author": "Jon Powell",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
